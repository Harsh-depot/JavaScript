// console.log(2 > 1);
// console.log(2 < 1);
// console.log(2 >= 1);
// console.log(2 <= 1);
// console.log(2 == 1);
// console.log(2 != 1);

// console.log("2" > 1);

// console.log("2" === 2);

// console.log(null == 0); (explanation: in this syntax equality operator is used so it will check the value only and null is not equal to 0 so it will return false, In this case the null is not converted into a munerical value)
//console.log(null > 0);  (explanation: in this syntax greater than operator is used so it will check the value and type of the variable, In this case the null is converted into a munerical value which is 0 and 0 is not greater than 0 so it will return false)
// console.log(null >= 0); (explanation: in this syntax greater than or equal to operator is used so it will check the value and type of the variable, In this case the null is converted into a munerical value which is 0 and 0 is equal to 0 so it will return true)

// console.log("0" === 0);
// === (explanation: strict equality operator checks both value and type)
