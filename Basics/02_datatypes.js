"use strict"; //this treats the code as of a newer version of JS, so that we can avoid some errors in the code

// alert("Hello World!");   //this will show a pop up message in the browser if used in browser console

console. log("Harsh")


let name = "harsh"
let age = 22
let isLoggedIn = false
let state;

// number => 2 to the power of 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique

// object

console.log(typeof "harsh" );
console.log(typeof null );
console.log(typeof undefined );